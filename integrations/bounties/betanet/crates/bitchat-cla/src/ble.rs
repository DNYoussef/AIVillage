//! BLE Convergence Layer Adapter (Stub)
//!
//! Placeholder implementation for BLE mesh networking.

/// BLE Convergence Layer Adapter
pub struct BleCla;

impl BleCla {
    pub fn new() -> Self {
        Self
    }
}

impl Default for BleCla {
    fn default() -> Self {
        Self::new()
    }
}
