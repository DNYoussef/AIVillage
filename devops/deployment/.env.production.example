# AIVillage Production Environment Variables
# This file contains all environment variables required for production deployment
# Copy this file to .env.production and fill in actual values

# =============================================================================
# CRITICAL: NEVER commit the actual .env.production file to version control
# =============================================================================

# Database Credentials
# These passwords should be strong, unique, and stored in your secrets management system
AIVILLAGE_POSTGRES_PASSWORD=your_secure_postgres_password_here
AIVILLAGE_REDIS_PASSWORD=your_secure_redis_password_here
AIVILLAGE_NEO4J_PASSWORD=your_secure_neo4j_password_here

# Application Security
# JWT secret must be at least 256 bits (32 characters) for production
AIVILLAGE_HYPERAG_JWT_SECRET=your_256bit_jwt_secret_key_here_32chars_minimum

# External API Keys
# Get these from your AI provider dashboards
AIVILLAGE_OPENAI_API_KEY=sk-your_openai_api_key_here
AIVILLAGE_ANTHROPIC_API_KEY=sk-ant-your_anthropic_api_key_here

# Monitoring & Observability
AIVILLAGE_GRAFANA_PASSWORD=your_secure_grafana_admin_password_here

# Gateway Security
# Generate these using: openssl rand -hex 32
AIVILLAGE_GATEWAY_API_KEY=your_gateway_api_key_here
AIVILLAGE_GATEWAY_SECRET_KEY=your_gateway_secret_key_here

# TLS Certificates
# Name of the Kubernetes secret containing TLS certificates
AIVILLAGE_TLS_SECRET_NAME=aivillage-production-tls

# Cogment Integration
# Database URL for Cogment metrics storage
AIVILLAGE_COGMENT_METRICS_DB_URL=postgresql://cogment_user:secure_password@postgresql:5432/cogment_metrics

# =============================================================================
# Security Notes:
# 1. Rotate all secrets every 90 days
# 2. Use your organization's secret management system (Vault, AWS Secrets Manager, etc.)
# 3. Never store production secrets in CI/CD logs or configuration files
# 4. Monitor secret access and usage through audit logs
# 5. Use principle of least privilege for secret access
# =============================================================================

# Secret Rotation Schedule:
# Database passwords: Every 90 days
# JWT secrets: Every 60 days
# API keys: As required by providers or security policy
# TLS certificates: Before expiration (typically yearly)

# Emergency Secret Revocation:
# 1. Immediately rotate the compromised secret
# 2. Update all deployments with new secret
# 3. Investigate potential breach
# 4. Review access logs for unauthorized usage
# 5. Update incident response documentation
